#!groovy
properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'master'
    }
    options {
        timestamps()
    }

    stages {
        stage("api-test") {
            steps {
                echo "api-test"
            }
        }
        stage("ui-test") {
            steps {
                echo "ui-test"
            }
        }
    }
}